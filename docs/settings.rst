.. _settings_intro:

========
Settings
========

These are the default settings you can override in your own project settings
right after the line which load these default settings.

.. Note::
    Until explicitely described, every path values are expected to be a ``pathlib.Path``
    object. Using simple path string could lead to failures or unexpected results.

.. automodule:: diskette.settings
   :members:
